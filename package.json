{
  "name": "portnest",
  "version": "1.1.0",
  "description": "Tauri desktop client for a dispatcher-managed SOCKS5 reverse tunnel",
  "author": "",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "start": "tauri dev",
    "check": "node --check src/renderer/tauri-bridge.js && node --check scripts/prepare-tauri-sidecars.js && node --check scripts/release-version.js && node --check scripts/write-runtime-config.js",
    "release": "node scripts/release-version.js",
    "tauri": "tauri",
    "tauri:prepare": "node scripts/prepare-tauri-sidecars.js",
    "tauri:check": "cargo check --manifest-path src-tauri/Cargo.toml",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  }
}
